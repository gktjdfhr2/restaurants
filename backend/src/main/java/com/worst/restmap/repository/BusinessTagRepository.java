package com.worst.restmap.repository;

import com.worst.restmap.domain.entity.BusinessTag;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BusinessTagRepository extends JpaRepository<BusinessTag, Long> {
}
