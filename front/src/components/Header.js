import React from 'react';

const Header =
  () => {
    console.log(
      'header',
    );
    return (
      <header>
        <section>
          <h2>
            header
          </h2>
        </section>
      </header>
    );
  };

export default Header;
